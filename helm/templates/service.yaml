apiVersion: v1
kind: Service
metadata:
  name: {{ include "pos-api.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "pos-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pos-api.selectorLabels" . | nindent 4 }}
status:
  loadBalancer: {}
